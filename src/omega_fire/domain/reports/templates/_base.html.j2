{# Template de base "cyber terminal" pour tous les rapports HTML Omega-Fire.

Centralise la palette (variables CSS) et les composants structurels
partagés (fenêtre terminal, titres, cartes de statistiques, tableaux,
badges de statut) — extraits par déduplication de audit_report.html.j2,
f2b_report.html.j2 et ruleset.html.j2, qui définissaient chacun une
copie quasi identique de ce même CSS avec des valeurs hex dupliquées.

Un template de rapport concret l'utilise via :

    {% extends "_base.html.j2" %}
    {% block title %}Mon rapport - Omega-Fire{% endblock %}
    {% block terminal_title %}omega-fire -- mon-rapport{% endblock %}
    {% block extra_style %}
        /* CSS propre à ce rapport uniquement (classes de contenu) */
    {% endblock %}
    {% block content %}
        <h1>Titre du rapport<span class="cursor"></span></h1>
        ...
    {% endblock %}

Convention : préfixe "_" = template partiel, jamais un rapport exportable
en soi (HtmlExporter.list_templates() ne doit pas le proposer comme choix
à l'utilisateur, à filtrer si cette liste est un jour exposée en UI).

Une seule famille de style pour l'instant ("cyber terminal", fond sombre,
police monospace) — un futur choix de thème d'export utiliserait un
template de base alternatif, sans toucher aux templates de contenu tant
qu'ils ne consomment que les classes/variables définies ici.
#}
<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Omega-Fire{% endblock %}</title>
    <style>
        {% include "_palette_" ~ theme_name|default("omega-base") ~ ".css.j2" %}

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg-body);
            color: var(--text-body);
            font-family: 'Fira Code', 'JetBrains Mono', Consolas, 'Courier New', monospace;
            line-height: 1.6;
            padding: 2rem 1.5rem;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .container {
            max-width: 1200px;
            width: 100%;
            background-color: var(--bg-container);
            border-radius: 6px;
            box-shadow: 0 20px 60px rgba(0, 20, 40, 0.8), 0 0 1px rgba(182, 212, 254, 0.2);
            border: 1px solid var(--border-default);
            overflow: hidden;
        }

        .terminal-header {
            background-color: var(--bg-header);
            padding: 0.6rem 1rem;
            border-bottom: 1px solid var(--border-header);
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .terminal-dots { display: flex; gap: 6px; }
        .dot { width: 10px; height: 10px; border-radius: 50%; }
        .dot-close { background-color: var(--dot-close-bg); border: 1px solid var(--dot-close-border); }
        .dot-min { background-color: var(--dot-min-bg); border: 1px solid var(--dot-min-border); }
        .dot-max { background-color: var(--dot-max-bg); border: 1px solid var(--dot-max-border); }
        .terminal-title {
            font-size: 0.75rem;
            color: var(--text-muted);
            margin-left: auto;
            margin-right: auto;
            letter-spacing: 0.5px;
        }
        .terminal-body { padding: 2.5rem 2.8rem; }

        h1 {
            font-size: 1.8rem;
            font-weight: 600;
            letter-spacing: -0.5px;
            color: var(--text-heading);
            border-bottom: 1px dashed var(--border-dashed);
            padding-bottom: 0.75rem;
            margin-bottom: 2rem;
            display: flex;
            align-items: center;
            gap: 12px;
        }
        h1::before { content: "$"; color: var(--text-accent); font-weight: 400; }
        .cursor {
            display: inline-block;
            width: 8px;
            height: 1.2em;
            background-color: var(--text-heading);
            margin-left: 6px;
            vertical-align: middle;
            animation: blink 1s infinite;
        }
        @keyframes blink { 0%, 100% { opacity: 1; } 50% { opacity: 0; } }

        h2 {
            font-size: 1.1rem;
            color: var(--text-subheading);
            margin: 2.2rem 0 1rem 0;
            padding-bottom: 0.4rem;
            border-bottom: 1px solid var(--border-default);
        }

        .note-historique {
            background-color: var(--bg-panel-alt);
            border: 1px solid var(--border-panel);
            border-left: 3px solid #2a4f7a;
            padding: 1.2rem 1.8rem;
            border-radius: 4px;
            color: var(--text-panel);
            margin-bottom: 2rem;
            font-size: 0.9rem;
        }
        .note-prefix { color: var(--text-muted); font-weight: bold; }

        .reco-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
            gap: 1.2rem;
            margin-top: 1rem;
        }
        .reco-item {
            background-color: var(--bg-panel);
            border: 1px solid var(--border-panel);
            border-radius: 4px;
            padding: 1.2rem 1.6rem;
        }
        .reco-item .label {
            font-weight: 600;
            color: var(--text-accent);
            display: block;
            margin-bottom: 0.3rem;
            font-size: 0.8rem;
            text-transform: uppercase;
        }
        .reco-item .label::before { content: "# "; color: #2a4f7a; }
        .reco-item .value { color: var(--text-value); font-size: 0.95rem; }

        .table-wrapper {
            overflow-x: auto;
            margin: 1.2rem 0 1.5rem 0;
            border-radius: 4px;
            border: 1px solid var(--border-default);
            background-color: var(--bg-table-wrapper);
        }
        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 0.85rem;
        }
        thead { background-color: var(--bg-header); border-bottom: 1px solid var(--border-header); }
        th {
            text-align: left;
            padding: 0.7rem 1rem;
            font-weight: 600;
            color: var(--text-subheading);
            letter-spacing: 0.5px;
            text-transform: uppercase;
        }
        td {
            padding: 0.6rem 1rem;
            border-bottom: 1px solid var(--border-cell);
            vertical-align: top;
            color: var(--text-table-cell);
        }
        tr:hover td { background-color: var(--bg-row-hover); color: var(--text-hover); }

        .badge {
            display: inline-block;
            padding: 0.15rem 0.55rem;
            border-radius: 3px;
            font-size: 0.75rem;
            font-weight: 600;
            letter-spacing: 0.3px;
        }
        .badge-success { background-color: var(--status-success-bg); color: var(--status-success-text); border: 1px solid var(--status-success-border); }
        .badge-warning { background-color: var(--status-warning-bg); color: var(--status-warning-text); border: 1px solid var(--status-warning-border); }
        .badge-neutral { background-color: var(--status-neutral-bg); color: var(--status-neutral-text); border: 1px solid var(--status-neutral-border); }
        .badge-danger { background-color: var(--status-danger-bg); color: var(--status-danger-text); border: 1px solid var(--status-danger-border); }
        .badge-info { background-color: var(--status-info-bg); color: var(--status-info-text); border: 1px solid var(--status-info-border); }

        .anomaly {
            padding: 0.8rem 1.2rem;
            border-radius: 4px;
            margin-bottom: 0.6rem;
            font-size: 0.88rem;
        }
        .anomaly-critical {
            background-color: var(--anomaly-critical-bg);
            border-left: 3px solid var(--status-danger-text);
            color: var(--anomaly-critical-text);
        }
        .anomaly-warning {
            background-color: var(--anomaly-warning-bg);
            border-left: 3px solid var(--status-warning-text);
            color: var(--anomaly-warning-text);
        }
        .anomaly-category {
            font-weight: 600;
            color: var(--text-heading);
            margin-right: 0.5rem;
        }

        .empty-state {
            text-align: center;
            padding: 1.5rem 1rem;
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .footer-note {
            margin-top: 2.5rem;
            padding-top: 1.5rem;
            border-top: 1px dashed var(--border-footer);
            color: var(--text-dim);
            font-size: 0.8rem;
            text-align: center;
        }

        {% block extra_style %}{% endblock %}
    </style>
</head>
<body>
    <div class="container">
        <div class="terminal-header">
            <div class="terminal-dots">
                <span class="dot dot-close"></span>
                <span class="dot dot-min"></span>
                <span class="dot dot-max"></span>
            </div>
            <span class="terminal-title">{% block terminal_title %}omega-fire{% endblock %}</span>
        </div>

        <div class="terminal-body">
            {% block content %}{% endblock %}
        </div>
    </div>
</body>
</html>
